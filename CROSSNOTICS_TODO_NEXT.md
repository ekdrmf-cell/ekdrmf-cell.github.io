# 천지인운명관 — 다음 세션 할 일 (2026-08-23 작성)

이 파일은 `CROSSNOTICS_HANDOFF.md`를 대체하지 않습니다 — 그 문서가 프로젝트 전체 이력을
담는 정식 인수인계 문서이니 **먼저 그 문서를 읽으세요.** 이 파일은 그 시점(2026-08-23
저녁)에 "지금 당장 다음에 뭘 해야 하는지"만 짧게 뽑아둔 실행용 체크리스트입니다.

## ⚠️ 새 세션 시작 전에 반드시 먼저 할 것

**`git status`와 `git log --oneline -15`부터 확인하세요.** 이 프로젝트는 여러 세션이
동시에 같은 파일(`crossnotics/index.html` 등)을 건드리는 일이 실제로 여러 번 있었습니다.
무작정 이어서 작업하지 말고, 그 사이 다른 세션이 뭘 커밋했는지/뭘 아직 안 커밋했는지부터
확인한 뒤 시작하세요.

**작성 시점 기준 상태**: 어떤 세션(별도 창, `천지인운명관 고객 질문 데이터베이스`)이
서비스허브와 천지인운명관을 코드상 완전히 분리하는 대규모 작업을 진행했고, 그 내용을
`CROSSNOTICS_HANDOFF.md`의 -11ㆍ-12번 항목에 이미 정리해뒀습니다. **그런데 `git status`로
확인해보니 그 변경사항(수십 개 파일 수정/삭제/신규, `CROSSNOTICS_HANDOFF.md` 자체 포함)이
아직 커밋되지 않고 워킹트리에만 남아있는 상태였습니다.** 다음 세션은 이걸 먼저 확인해서:
- 그대로 남아있으면 그 세션이 마저 커밋하기를 기다리거나, 사용자에게 상태를 확인한 뒤
  검토해서 커밋할 것.
- 이미 커밋됐으면 이 경고는 무시하고 진행.

## 이번 세션(2026-08-23)이 완료ㆍpush한 것

`crossnotics/index.html`, `crossnotics/css/crossnotics.css`, `crossnotics/privacy.html`,
`crossnotics/apps-script/Code.gs` 관련, 커밋 `10026cb`~`7631728` (총 7개 커밋, 전부
`origin/main`에 push 완료):

1. 가격 비교표(`가격대별로 뭐가 다른가요`) 삭제 → 각 가격 카드 안에 스펙 항목으로 통합
2. "천지인운명관이 지키는 원칙" 섹션 삭제 (**새 문구는 아직 안 정함 — 아래 TODO 참고**)
3. 신뢰 배지ㆍ중간 CTAㆍFAQㆍ환불 불가 정책 명시
4. 다크 럭셔리 GUI 리디자인(글래스모피즘, 노이즈 텍스처, 장식 구분선, 스크롤 등장 애니메이션,
   컬러풀한 이모지 전부 제거)
5. 상품 카드 클릭 → 신청폼 자동 스크롤
6. **mailto 팝업 완전 제거** → Google Apps Script(사장님 본인 구글 계정, `crossnotics/
   apps-script/Code.gs`, 배포 URL은 `crossnotics/index.html`의 `CN_MAIL_ENDPOINT`)로 조용히
   접수 후 "신청이 접수되었습니다" 화면 표시. 실제 브라우저 제출 → Gmail 수신까지 전체
   파이프라인 검증 완료.
7. 개인정보처리방침을 서비스허브 루트 대신 `crossnotics/privacy.html` 자체 페이지로 분리
8. 질문칸 placeholder를 `question_taxonomy.md` 기준 "direct(확실히 답변 가능)" 유형으로 교체
9. 신청폼의 "정보 제공 목적 동의" 체크박스 제거(하단 고지문과 중복이라 삭제 지시받음)

## 다음에 해야 할 일 (미해결, 우선순위 순 아님)

- [ ] **"지키는 원칙" 섹션 새 문구 작성** — 삭제만 하고 아직 뭘로 채울지 안 정함. 사용자가
  "그건 지금 정할 게 아니야"라고 보류함. 방향(같은 형식 유지 vs 완전히 다른 형식) 다시 물어볼 것.
- [ ] **띠별 운세를 유료 crossnotics 리포트에도 추가할지 결정** — `천지인운명관 소셜 미디어
  홍보` 세션에서 나온 미해결 질문. 지금은 무료 사주 계산기(`saju/`)에만 있고 유료 리포트
  계산 엔진(`tools/crossnotics-engine/saju.js`)엔 띠 계산ㆍ리포트 항목이 없음. 추가한다면
  어느 티어부터 넣을지도 같이 정해야 함.
- [ ] **파비콘 분리** — 아직 서비스허브와 공유 중(`favicon.svg`/`apple-touch-icon.png`).
  천지인운명관 전용 브랜드 자산으로 새로 만들지 결정 필요.
- [ ] **Google Analytics 속성 분리** — 지금 GA 추적 ID(G-NR3D3FD597)가 서비스허브와 공유
  중. 완전히 분리하려면 사용자가 자기 구글 계정에서 새 GA4 속성을 만들어야 함(계정 작업,
  Apps Script 배포 때처럼 사용자가 직접 몇 단계 진행 필요).
- [ ] **URL 경로(`/crossnotics/`) 완전 독립 여부** — 지금은 같은 GitHub 저장소ㆍ같은
  `ekdrmf-cell.github.io` 도메인 아래 `/crossnotics/` 경로만 씀. 완전히 별도 저장소/도메인으로
  옮길지는 사용자가 아직 결정 안 함(옵션으로만 남겨둠).
- [ ] **위 "새 세션 시작 전에" 항목의 대규모 미커밋 변경사항** — 검토 후 커밋할 것(이번
  세션은 그 파일들을 건드리지 않아서 안전하게 남겨뒀음).

## 참고 — 관련 세션 (같은 사용자, 다른 창)

- **`천지인운명관 고객 질문 데이터베이스`** (`C:\Users\ekdrm\OneDrive\Desktop\천지인운명관`) —
  서비스허브ㆍ천지인운명관 분리 작업, 명리학 대응표ㆍ궁합 엔진ㆍ신살 계산 등 대부분의
  백엔드 로직 작업이 여기서 진행됨.
- **`천지인운명관 소셜 미디어 홍보`** — SNS 마케팅 리서치, "천지인 SNS 실전 가이드" 아티팩트
  제작 완료(2주 콘텐츠 캘린더 포함). 링크는 이 세션 대화 기록 참고.
