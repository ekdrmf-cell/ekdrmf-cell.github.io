# 배포·수익화 인프라 인수인계 — 새 세션에서 이어받을 때 읽는 문서

**최종 갱신**: 2026-08-11
**용도**: 전자책 제작(`EBOOK_HANDOFF.md`)과는 별개 트랙. "사이트를 실제로 배포하고 광고·결제를
붙여서 진짜 돈을 벌기 시작하는" 작업의 진행상황을 정리한 문서. 새 세션에서 이어받으면
이 문서부터 읽을 것.

**중요 — 사이트 이름이 바뀜(2026-08-11)**: "수익화허브" → **"서비스허브"**로 변경됨. 이
문서 아래쪽 내용(2026-08-03 이전)은 전부 옛 이름으로 쓰여있지만 같은 사이트를 가리키는
것임. 실제 주소는 그대로 `https://ekdrmf-cell.github.io`.

## 0-1. 2026-08-11 추가 작업

- **구글 애널리틱스(GA4) 연결**: 측정 ID `G-NR3D3FD597`. 전 페이지(index/services/ebooks/
  privacy/games 7종, 총 11개 파일) `<head>`에 gtag.js 스니펫 삽입 완료, "태그 감지됨" 확인함.
  **보고 시간대를 대한민국(KST)으로 변경 완료**(기본값 미국이면 하루 집계 기준이 안 맞음).
  다음 세션에서 실제 방문자 수 확인하려면 analytics.google.com 로그인 후 확인.
- **사이트 이름 변경**: "수익화허브" → "서비스허브". `js/common.js`(헤더/푸터 로고, 전
  페이지 공통)ㆍ각 페이지 `<title>`ㆍ`privacy.html` 본문 전부 일괄 변경.
- **파비콘 추가**: 브라우저 탭에 기본 "E" 아이콘이 뜨던 문제 발견 → `js/common.js`의
  기존 로고 디자인(보라 그라디언트 둥근사각형+흰 노드 아이콘)과 똑같은 모양으로
  `favicon.svg`/`favicon.ico`/`apple-touch-icon.png` 제작해서 전 페이지에 연결.
- **검색 노출 관련 사용자 질문에 답변**: 새 사이트라 "서비스허브" 같은 브랜드명 검색으로도
  아직 안 뜰 수 있음을 정직하게 안내함. 다음 단계로 **구글 서치콘솔**과 **네이버
  서치어드바이저** 등록을 제안함(둘 다 로그인/소유확인은 사용자가 직접, 소유확인 코드가
  나오면 이어서 사이트에 심어주면 됨) — **다음 세션에서 사용자가 이어서 진행하면 이 등록
  절차를 마무리할 것.**

---

## 0. 지금까지 완료한 것 (2026-08-03, 배관공사 완료)

1. **GitHub 배포 완료**: `git push`가 반복 타임아웃되던 문제 해결 — 사용자가 직접 터미널에서
   포그라운드로 `git push` 실행 → 브라우저 GCM 로그인 창에서 "Authorize" 클릭 → 성공.
   (교훈: 에이전트가 백그라운드로 push를 실행하면 브라우저 로그인 창이 떴는지 알 수 없어서
   계속 실패했던 것 — **push는 항상 사용자가 직접 포그라운드 터미널에서 실행하고 로그인
   창을 직접 봐야 함.** 이후 config.js 등 후속 push는 Bash 도구로도 정상 동작함.)
2. **GitHub Pages 배포**: 저장소를 `akdlfem` → `ekdrmf-cell.github.io`로 **이름 변경**
   (애드센스가 도메인 루트 검증을 요구해서, 하위경로 프로젝트 페이지 대신 User Page로 전환).
   현재 실제 서비스 주소: **https://ekdrmf-cell.github.io**
3. **애드센스 사이트 등록 + 검토 요청 완료**: 퍼블리셔 ID `ca-pub-9038430968074722`를
   `js/config.js`에 반영. 검증 방식은 "메타 태그"(index.html `<head>`에 직접 삽입, 코드
   스니펫 방식은 JS 동적 삽입이라 크롤러가 못 잡아서 실패했었음 — 정적 메타태그가 확실함).
   동의 메시지(EEA/UK/스위스용 CMP)도 설정 완료. **지금은 구글 심사 대기 중**(며칠~몇 주
   소요, 결과는 이메일로 옴). 승인되면 별도로 할 일 없음(이미 스크립트 연결까지 다 돼있음).
4. **결제(계좌 안내) 흐름 완성**: 토스 `toss.me` 개인링크는 **서비스 종료됨**(2026-08-03 확인,
   사이트 자체가 종료 안내 문구만 뜸). 대안으로 **계좌 안내 팝업 방식**을 새로 구현:
   - 계좌: 케이뱅크 100-137-259635 (예금주: 최광호), `js/config.js`의 `paymentGuideText`에 설정.
   - 구매 버튼 클릭 → `showPaymentGuide()`가 계좌 안내 모달을 띄움 → "입금 완료, 문의 이메일
     보내기" 클릭 시 기존 `contactMail()`로 이어짐. `js/config.js`에 신규 구현, `js/ad-stub.js`의
     게임 광고제거 구매 흐름(`showRemoveAdsPurchase`)도 `paymentGuideText`를 인식하도록 반영.
   - 실 사이트에서 전자책 구매 버튼 클릭해 팝업 정상 작동 확인 완료(스크린샷으로 검증).
5. **연락처**: `ekdrmf@gmail.com` 개인 이메일을 계속 쓰기로 사용자가 확정함(전용 채널로
   안 바꿈). config.js의 관련 TODO 주석 제거함.
6. **구매 문의 자동화(반자동)**: `tools/gmail-auto-reply/AutoReply.gs` 신규 작성.
   - **왜 완전자동이 아닌가**: 사업자등록 없이는 은행 API 연동이 불가능해서, "진짜 입금됐는지"는
     사람이 케이뱅크 앱으로 직접 확인하는 수밖에 없음. 확인 없이 이메일만 보고 자동발송하면
     아무나 무료로 전자책을 받아갈 수 있어 위험함.
   - **자동화된 부분**: Gmail에 "OO 구매 문의" 메일이 오면(15분 트리거), 제목 키워드로 상품을
     판별해 구글드라이브 폴더(`수익화허브_전자책`)에서 해당 파일을 찾아 첨부한 **답장 초안**을
     Gmail 임시보관함에 자동으로 만들어둠. 발송 버튼은 사람이 입금 확인 후 직접 클릭.
   - **설치 완료(2026-08-03)**: 구글드라이브 "수익화허브_전자책" 폴더에 8개 파일 업로드,
     script.google.com에 `AutoReply.gs` 붙여넣고 실행 권한 승인(Gmail 전체/Drive 접근),
     15분마다 `checkPurchaseInquiries`를 실행하는 시간 기반 트리거까지 등록 완료.
     테스트 실행 로그 "검사 대상 문의 0건" 정상 확인(오류 없음). **완전히 작동 중.**
     (권한 승인 화면에서 "팝업이 차단됨" 에러가 한 번 났었음 — 브라우저 팝업 차단 허용
     후 재시도하니 해결됨. 나중에 비슷한 앱스크립트 설치할 때 참고.)

## 1. 배관공사 상태 — 2026-08-03 기준 전부 완료

지금 시점에 에이전트가 더 할 수 있는 일은 없음. 남은 건 전부 시간이 걸리거나(애드센스
심사) 실제 손님이 생겨야 발생하는 일(구매 문의 처리):

1. **애드센스 승인 대기**: 결과 오면 광고가 자동으로 뜨기 시작함(코드는 이미 다 준비됨,
   추가 작업 없음). 거절되면 사유 확인 후 재신청.
2. **실제 판매 흐름 확인**: 진짜 손님이 구매 문의를 보내면, 15분 안에 Gmail 임시보관함에
   초안이 잘 만들어지는지 최초 1회는 직접 확인해볼 것(로직은 검증됐지만 실제 메일로는
   아직 테스트 안 함).
3. 이후 확장 아이디어(우선순위 낮음, 급하지 않음): 게임 리워드/전면 광고 단위 ID 설정
   (`rewardAdSlot`, `interstitialAdSlot`), 책8 제작(`EBOOK_HANDOFF.md` 참고), 게임 장르 추가.

## 2. 다음 세션 시작 프롬프트

"수익화허브 배관공사 이어서 하자"라고 말하면 이 문서를 먼저 읽고 1번 목록부터 확인하며
진행하면 됨. `EBOOK_HANDOFF.md`(전자책 제작 트랙)와는 독립적인 작업이므로 서로 순서를
신경 쓸 필요 없음.
