# 천지인운명관 — 만든 방법ㆍ운영 방법ㆍ수익화 가이드

2026-08-23 작성. 이 문서는 "어떻게 만들었는지ㆍ어떻게 돌리는지ㆍ어떻게 돈을 버는지ㆍ어떻게
파는지"를 한 곳에 정리한 사업 운영 문서입니다. 세부 개발 이력(어떤 버그를 언제 고쳤는지 등)은
`CROSSNOTICS_HANDOFF.md`에 시간순으로 남아있으니, 코드를 직접 고칠 땐 그 문서를 참고하세요.
여기서는 "사업을 어떻게 돌리는가"만 다룹니다.

---

## 1. 이게 뭔가

**천지인운명관**은 사주ㆍ서양점성술ㆍ타로 세 체계를 각각 독립적으로 계산한 뒤, 어디서
서로 일치하는지를 교차검증하는 개인 맞춤 진단 서비스입니다.

**핵심 차별점(가격 정당화의 근거)**: 시중 사주 서비스 대부분은 AI가 사주 지식을 이용해
글을 통째로 "창작"합니다. 우리는 반대입니다 — 사주ㆍ점성술ㆍ타로를 각각 **검증된 계산
라이브러리로 먼저 확정된 값을 뽑고**, AI(LLM)는 그 계산 결과를 문장으로 옮기는 역할만
합니다. 계산에 없는 숫자ㆍ카드ㆍ간지ㆍ통계를 지어내지 않는다는 게 판매 문구가 아니라
실제 코드 구조입니다(3번 참고).

---

## 2. 어떻게 만들었나 — 기술 구조

### 2-1. 계산 엔진 (`tools/crossnotics-engine/`, Node.js)

| 파일 | 역할 |
|---|---|
| `saju.js` | lunar-javascript(오픈소스)로 십신ㆍ지장간ㆍ12운성ㆍ공망ㆍ대운(8구간)ㆍ세운까지 계산. 음력 생일도 자동 양력 환산. |
| `astrology.js` | circular-natal-horoscope-js로 행성 10개(태양~명왕성)ㆍ하우스ㆍ어스펙트 계산. |
| `tarot.js` | 셔플 + 3장/켈틱크로스(10장) 스프레드. 78장 전체 덱. |
| `correlate.js` | 사주 오행ㆍ점성술 4원소ㆍ타로를 공통 좌표계로 정규화해 "체계 일치도(%)"를 계산 — 이 프로젝트의 핵심 알고리즘. |
| `correspondence.js` | 오행별 색ㆍ방향ㆍ숫자ㆍ맛ㆍ신체장기, 띠, 십신 기반 직업 적성 등 명리학 표준 대응표. 리포트 프롬프트 연동은 아직 미완(7번 참고). |
| `gunghap.js` | 두 사람의 사주 궁합 점수화(일간ㆍ일지ㆍ년지 관계 + 오행 상호보완). `intake.customer.partner`에 상대방 생년월일이 있으면 `run.js`가 자동 계산. 리포트 프롬프트ㆍ폼 입력칸 연동은 아직 미완(7번 참고). |
| `run.js` | CLI 진입점. `node run.js intake.json computed.json`으로 위 전부를 한 번에 실행. |

이 단계는 **100% 결정론적 계산**입니다 — AI가 전혀 관여하지 않고, 같은 입력이면 항상 같은
결과가 나옵니다.

### 2-2. 리포트 생성 (`tools/crossnotics-report/`, Python)

| 파일 | 역할 |
|---|---|
| `build_report.py` | `computed.json`(위 계산 결과)을 Anthropic API에 넣어 **문장만** 생성. 강제 tool-call 스키마로 "계산값 없는 건 절대 못 쓰게" 구조적으로 막음. |
| `report_kit.py` | 생성된 문장 + 계산값을 실제 브랜드 PDF로 변환(목차, 지표 막대그래프, 인용구 강조, 액션플랜 등 시각 요소 다수). |
| `catalog_names.py` | PDF 표지에 들어갈 상품명(예: "천지인운명관 MASTER — ...") 매핑. |

리포트는 손님 질문마다 `direct`(직접 계산 가능)ㆍ`redirected`(부분 답변)ㆍ`unanswerable`
(원천 불가) 중 하나로 판정한 뒤, 답 못 준 부분이 있으면 **절대 숨기지 않고 이유를 먼저
밝히고** 관련된 실제 내용을 이어붙입니다(4-3번 참고).

### 2-3. 손님용 웹사이트 (`crossnotics/index.html`)

- 다크 톤 + 금색 포인트 디자인(어둡고 고급스러운 느낌으로 재설계됨).
- **가격대별 비교표**(`#cn-compare-table`) — 6개 티어를 표로 나열해 "가격이 오를 때 정확히
  뭐가 늘어나는지" 한눈에 보이게 함.
- **리포트 미리보기 섹션** — 결제 전에 실제 계산 항목 형식 그대로의 예시(가상 인물)를 보여줌.
- 폼 제출 시 `contactMail()`(무료 티어) 또는 `contactPurchase()`(유료 티어, 계좌이체 안내
  팝업)로 연결 — 결제 자동화는 아직 없음(6번 참고).

### 2-4. 가격표의 유일한 기준

`site-checkout/lib/catalog.js`의 `CROSSNOTICS_TIERS`가 가격ㆍ상품명ㆍ분량의 **단일
소스**입니다. Node(`run.js`)와 웹사이트(`crossnotics/index.html`)가 이 값을 각자
참조/복사해서 씁니다 — **가격이나 티어를 바꿀 땐 `catalog.js`를 먼저 고치고, 그다음
`crossnotics/index.html`의 `CN_TIERS` 배열도 손으로 맞춰야 합니다**(자동 동기화 안 됨).

---

## 3. 상품 구성 (2026-08-23 기준, 확정 수치)

| 티어 | 가격 | 분량 | 체계 | 질문 개수 | 특징 |
|---|---|---|---|---|---|
| **FREE** | 무료 | 1페이지 | 사주 | 0개 | 진입 장벽 없는 리드 확보용 |
| **LIGHT** | 30,000원 | 2페이지 | 사주 | 1개 | 현재 대운 구간만 |
| **SINGLE** | 50,000원 | 6페이지 | 사주 | 3개 | 대운 전체 8구간 |
| **DUAL** | 100,000원 | 13페이지 | 사주+별자리 | 6개 | 두 체계 교차검증 |
| **MASTER** | 150,000원 | 20페이지 | 사주+별자리+타로 | 10개 | 켈틱크로스 10장, 기회ㆍ리스크 분석, 실전 액션 플랜 |
| **PREMIUM** | 200,000원 | 30페이지 | 사주+별자리+타로 | 12개 | MASTER 전체 + 대운 8구간 10년 로드맵ㆍ평생 설계ㆍ인생 2막 |

**가격 논리**: "체계 개수"(사주 1개 → 사주+별자리 2개 → 3체계 전부)가 1차 가격 기준이고,
질문 개수ㆍ분량은 상위 티어일수록 딸려오는 혜택입니다. 순수 질문개수제(질문 몇 개=얼마)는
검토했다가 기각했습니다 — 질문당 단가가 평평해져 업셀 유인이 없고, 이 서비스의 핵심 가치
(교차검증)가 가격표에서 안 드러나기 때문입니다.

**주의**: 분량(페이지 수)은 "약"ㆍ"목표" 같은 애매한 표현 없이 확정 수치로 표기하고 있지만,
LLM이 실제로 생성하는 글의 길이가 항상 그 숫자에 딱 맞는다는 보장은 없습니다. **발송 전
사람이 실제 PDF 페이지 수를 이 표와 대조해서 확인하는 절차가 반드시 필요합니다**(아직
코드로 자동 강제되지 않음).

---

## 4. 어떻게 운영하나 (주문 처리 파이프라인)

### 4-1. 주문이 들어오면 (수동 파이프라인, 지금 방식)

1. 손님이 `crossnotics/index.html`에서 티어 선택 → 생년월일시 등 입력 → 제출.
2. 무료 티어는 바로 이메일 문의로, 유료 티어는 계좌이체 안내 팝업 → 입금 확인 후 이메일로
   `intake.json` 형태의 JSON이 옴(폼이 자동으로 만들어서 이메일 본문에 넣어줌).
3. 그 JSON을 파일로 저장한 뒤 순서대로 실행:
   ```bash
   node tools/crossnotics-engine/run.js 주문.json computed.json
   python tools/crossnotics-report/build_report.py computed.json
   python tools/crossnotics-report/report_kit.py computed.json computed.report.json 결과.pdf
   ```
4. **발송 전 사람이 직접 확인**: PDF를 열어서 (a) 페이지 수가 3번 표와 맞는지, (b)
   `build_report.py` 실행 시 뜬 경고(⚠, 특히 "%" 문자 발견ㆍ연도 불일치ㆍ별자리 용어 불일치)가
   없는지, (c) 질문 답변 중 `redirected`ㆍ`unanswerable`로 처리된 게 있다면 그 설명이
   자연스러운지 확인.
5. PDF를 손님 이메일로 첨부 발송.

### 4-2. 비용 구조 (실측치, 2026-08-21 확인)

- SINGLE 티어 리포트 1건 생성 비용: **약 33~50원**(Anthropic API 실비)
- MASTER 티어 1건: **약 150~250원**
- PREMIUM은 분량이 더 커서 250~400원 선으로 추정(아직 실측 안 함)

판매가(5만~20만원) 대비 원가는 무시할 수준입니다 — **마진은 사실상 API 비용이 아니라
"손님이 진짜 만족하는 리포트를 만드는 것"에 달려있습니다.**

### 4-3. 질문 답변 처리 원칙 (중요 — 반드시 지킬 것)

손님이 남긴 질문마다 리포트가 자동으로 세 가지 중 하나로 판정합니다:

- **direct**: 계산된 데이터로 바로 답변(대부분의 질문 — 이직운ㆍ연애운ㆍ이 시기 조심할 점 등)
- **redirected**: 문자 그대로는 계산 불가(예: 상대방 정보 없는 궁합 질문)하지만, 그 이유를
  먼저 밝히고 관련된 실제 데이터로 답변
- **unanswerable**: 원천적으로 어떤 방법으로도 계산 불가(로또 번호, 정확한 사망 시점 등) —
  이유를 설명하고 지어내지 않음

**절대 원칙**: 답을 못 준 사실을 숨기고 다른 내용을 원래 질문의 답인 것처럼 위장하지
않습니다. 이게 이 서비스의 정직성 원칙의 핵심이고, 타사(운명도감 등) 벤치마킹에서 실제로
발견한 "지어낸 통계ㆍ지어낸 예언" 문제를 반복하지 않기 위한 장치입니다.

`tools/crossnotics-report/logs/question_answerability_log.jsonl`에 모든 질문과 판정이
쌓입니다. `python build_report.py --log-summary`로 실제 비율을 확인할 수 있습니다 —
**"답 못 하는 질문이 얼마나 되는지"는 추측하지 말고 이 로그로 확인하세요.** (이 로그
폴더는 손님 질문 원문이 담겨 있어 `.gitignore`에 등록되어 있고 절대 커밋되면 안 됩니다.)

### 4-4. 지금 계산 가능한 것 / 아직 안 되는 것

| 질문 유형 | 상태 |
|---|---|
| 이직운ㆍ연애운ㆍ재물운ㆍ이 시기 조심할 점 | ✅ 계산 가능(direct) |
| 대운 흐름ㆍ성향ㆍ타고난 기질 | ✅ 계산 가능(direct) |
| 궁합(상대방과의 관계) | 🟡 **계산 엔진은 완성됨**(`gunghap.js` — 일간ㆍ일지ㆍ년지 관계+오행 상호보완으로 점수화). 다만 폼에 상대방 생년월일 입력칸이 없고, 리포트 프롬프트가 이 데이터를 아직 안 씀 — 마지막 연결(7번 참고) 전까지는 여전히 redirected로 처리됨 |
| 띠, 오행별 색ㆍ방향ㆍ음식ㆍ숫자ㆍ신체장기ㆍ직업 적성 | 🟡 **대응표는 완성됨**(`correspondence.js`). 리포트 프롬프트 연동 전까지는 direct로 안 올라감(7번 참고) |
| 로또 번호, 정확한 사망 시점 등 | ❌ 원천적으로 불가능(unanswerable) — 어떤 방법으로도 답 못 함 |

**주의**: 위 🟡 두 항목은 2026-08-23 오후에 `tools/crossnotics-engine/`에 계산 로직
자체는 이미 만들어졌습니다(`gunghap.js`, `correspondence.js`, `saju.js`/`run.js`에 연동
완료, `node test/run-all.js` 통과 확인). **다만 이 문서를 작성하는 시점 기준으로 아직
안 끝난 부분**: (1) 손님용 폼(`crossnotics/index.html`)에 상대방 생년월일 입력칸이 없고,
(2) `build_report.py`의 SYSTEM_PROMPT가 이 새 필드(`gunghap`ㆍ`partner_saju`ㆍ대응표
데이터)를 어떻게 써야 하는지 아직 모르고, (3) `catalog.js` 상품 설명에도 반영 안 됨.
계산은 다 됐는데 "마지막 연결"만 안 된 상태입니다.

---

## 5. 수익화 전략

1. **저가 진입(FREE~LIGHT)으로 리드 확보** → **고가 티어(MASTER~PREMIUM)로 업셀**하는
   구조. 무료 티어는 계산은 되지만 짧게(1페이지) 줘서, 신뢰는 쌓되 상위 티어를 잠식하지
   않게 설계됨.
2. **가격은 "체계 개수 × 분량 × 특수 섹션 유무"로 정당화** — 3번 표를 손님이 결제 전에
   직접 비교할 수 있게(비교표) 만들어서 "왜 비싼지" 스스로 납득하게 함.
3. **원가가 극히 낮아(건당 수백 원) 마진 구조가 좋음** — 문제는 원가가 아니라 전환율과
   손님 만족도. 그래서 리포트 품질(분량ㆍ통찰 깊이ㆍ가독성)에 투자하는 게 원가 절감보다
   훨씬 중요.
4. **재구매/교차판매**: 손님이 FREE/LIGHT로 시작했다가 SINGLE 이상으로 업그레이드하거나,
   `services.html`의 다른 무료 도구(사주ㆍ타로 등)에서 업셀 CTA로 유입.

---

## 6. 판매ㆍ홍보 전략

**결제는 계좌이체로 시작**(카드결제 자동화 `site-checkout/`은 코드는 완성되어 있지만
보류 중 — 주문량이 늘면 그때 연결).

**콘텐츠/유입 성장 엔진**(승인된 계획서 기반, 아직 실행 전):
1. 인스타그램 릴스 업로드(사주 관련 콘텐츠, AI 아바타 영상 검토 중).
2. 댓글 → 인스타그램 자체 "자동화" 기능으로 자동 DM 발송(Meta Business Suite에서 설정 —
   **계정 소유자만 설정 가능, 대신 못 해줌**). DM 링크는 크로스노틱스 사이트로 연결.
3. 카카오톡은 완전자동화 대신 예외 문의ㆍAS용 수동 채널로만 유지.
4. `services.html`에 등재, 무료 사주/타로 도구 결과 화면에 업셀 CTA 연결(이미 되어있음).

**아직 사용자가 직접 해야 하는 일**:
- Instagram "댓글→자동DM" 자동화 설정
- Vrew 등 아바타 영상 도구 실제 기능 확인
- (선택) 포트원 가입 + Vercel 배포로 카드결제 자동화 전환

---

## 7. 다음에 할 일 (우선순위순)

1. **궁합ㆍ대응표 "마지막 연결" 마무리** — 계산 엔진(`gunghap.js`, `correspondence.js`)은
   완성ㆍ테스트 통과 상태(4-4번 참고). 남은 건 배관 연결뿐:
   - 폼(`crossnotics/index.html`)에 상대방 생년월일시 입력칸 추가(선택 필드로, 궁합
     질문을 쓸 손님만 채우게)
   - `build_report.py`의 SYSTEM_PROMPT/스키마에 `gunghap`ㆍ`partner_saju`ㆍ대응표 필드
     사용법 추가 — 이게 되면 궁합ㆍ띠ㆍ색ㆍ방향 질문이 "redirected"에서 "direct"로 승격됨
   - `report_kit.py`에 궁합 점수ㆍ대응표 시각 요소(막대ㆍ배지 등) 렌더링 추가
   - `catalog.js` 상품 설명에도 반영, 어느 티어부터 궁합 질문을 받을지 결정
2. **첫 실주문 실제 API 테스트** — 지금까지 전부 목업 데이터로만 검증했습니다. 실제
   API로 최소 1건씩(SINGLE/MASTER/PREMIUM, 가능하면 궁합 질문 포함) 생성해서 (a) 페이지
   수가 3번 표와 맞는지, (b) 실제로 통찰 깊이가 있는지, (c) 궁합ㆍ대응표 데이터가
   자연스럽게 녹아드는지 사람이 읽어서 확인 필요.
3. **결제ㆍ발송 전 체크리스트 문서화** — 4-1번 절차를 실제 운영 매뉴얼로 다듬기.
4. (선택) 카드결제 자동화, 릴스ㆍDM 마케팅 실행.

---

## 8. 참고 파일 위치

- 가격ㆍ상품 단일 소스: `site-checkout/lib/catalog.js`
- 계산 엔진: `tools/crossnotics-engine/`
- 리포트 생성: `tools/crossnotics-report/`
- 손님용 사이트: `crossnotics/index.html`, `crossnotics/css/crossnotics.css`
- 개발 이력 전체(시간순, 세부 버그ㆍ결정 기록): `CROSSNOTICS_HANDOFF.md`
- 최초 승인된 설계 전체: `C:\Users\ekdrm\.claude\plans\idempotent-rolling-hopcroft.md`
